# ctower-runner boundary

Future Python worker-plane composition root. It composes separately versioned Harness, Supervisor, Target, Workspace, and Telemetry Adapters through `ctower-runner-sdk`. It receives fenced jobs and returns observations; it never owns ticket, workflow, gate, or effect truth. Product code begins in Increment 2.
