# ctowerctl boundary

Future Python CLI over the generated command/query client. Its offline spool is transport durability, not another ticket database. It cannot bypass server authorization, validation, readiness, evidence, or gate rules. Product code begins with CT-I1-004.
