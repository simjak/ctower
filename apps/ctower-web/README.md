# ctower-web boundary

Strict TypeScript browser client for exactly Home, Board, Ticket, Fleet, and Analytics. The browser uses the generated TypeScript API client and holds no authority or API token. A frontend framework is deliberately unselected until its L0 review.
