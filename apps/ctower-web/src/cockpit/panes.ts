/**
 * Both headed panes carry a head of the same height, so the rule under them is
 * one line across the cockpit rather than a step. It is one value because it is
 * one horizon. The crew rail has no head — its column is the list itself.
 */
export const PANE_HEAD = "flex h-15 shrink-0 items-center border-b border-line";
