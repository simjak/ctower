# ctower-systemd-vps boundary

Future narrow Effect Provider Adapter and separately supervised release helper. It accepts short-lived allowlisted grants and emits hash-chained receipts. It never decides authorization or release truth. A deterministic fake and a live implementation must pass the same contract before Increment 2 exits.
