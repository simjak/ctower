# Example goals

Company goals are durable business outcomes referenced by tickets. This docs-first example intentionally defines no live goal or ticket identity; those are created through authenticated commands once the control plane exists.
