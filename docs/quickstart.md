# Quickstart

The executable quickstart now lives in [Getting started](getting-started.md). It is agent-first, uses one
command, owns a disposable PostgreSQL/API/CLI fixture, and cleans up after proving the full workflow.

Use the [CLI reference](reference/cli.md) when you need the exact supported surface or the explicit
project/team gaps.
