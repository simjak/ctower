# Recovery invariants, not a complete postgresql.conf.
archive_mode = on
archive_command = 'false' # CP3-D replaces this only after pgBackRest archive-push proof.
archive_timeout = '60s'
wal_level = replica

# A recovered installation starts read-quarantined. Effects are never enabled in I1.
default_transaction_read_only = on
