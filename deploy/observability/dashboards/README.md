# Dashboard contracts

Dashboards will cover command latency/durability, projection/outbox completeness, runner recovery, protected-effect reconciliation, incident detection, and backup/restore evidence. Every panel must declare units, low-cardinality dimensions, source contract revision, unknown-state behavior, and recovery owner.
